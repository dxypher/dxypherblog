%ul.span2.categories.nav.navlist
  %h2.nav-header
    %i.icon-th-list
    Categories
  %li.active= link_to "All Articles", blog_filter_path(category: "all"), remote: true
  - @categories.each do |category|
    %li= link_to category.title, blog_filter_path(category: category), remote: true